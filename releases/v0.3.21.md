# UGS v0.3.21

This release adds the generated high-trust bootstrap profile.

High-trust initialization installs public signer roles, allowed and revoked
signer registries, and signature validators. It never generates or packages
private keys, and normal initialization requires an operator-configured SSH
signing key.
