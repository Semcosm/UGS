# UGS v0.3.19

This release adds the generated standard bootstrap profile.

The bootstrap package now supports:

- `ugs init --profile baseline`
- `ugs init --profile standard`
- standard quality, supply-chain, repository-shape, and SHA-pinned workflow scaffolding

The Release workflow validates the package from a clean runner and verifies
the published asset before initializing a disposable repository.
