# UGS v0.3.13

**Release status:** formal release.

**Specification:** UGS v0.3 remains the active policy and conformance
profile. This superseding patch fixes evidence path validation.

## Summary

Correct the evidence-path jq validation expression and add stability coverage
for declaration-only manifests, valid evidence paths, and traversal attempts.

## Compatibility

Valid declarations remain valid. The fix rejects malformed or unsafe evidence
paths consistently; v0.2 history and published tags are unchanged.

## Verification

Run `scripts/validate_repo.sh`, `scripts/ugs_check.sh --format json`, and the
stability audit covering optional supply-chain declarations. Verify this
release with `scripts/validate_release_tag.sh v0.3.13`.

## Rollback

Do not delete or replace published tags. Publish a superseding patch release
if a further correction is needed.
