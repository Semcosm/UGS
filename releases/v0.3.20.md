# UGS v0.3.20

This release strengthens the bootstrap Release consumer test.

The remote consumer now downloads the published archive and verifies both:

- baseline repository initialization;
- standard repository initialization and profile validators.
