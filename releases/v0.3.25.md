# UGS v0.3.25

This superseding patch release includes the final bare-Git receive/update
adapter corrections and audit coverage records following v0.3.24.

UGS Core remains runnable without GitHub APIs, event files, URLs, or `.github`
files. GitHub PR, Actions, and Releases remain explicit compatibility adapters,
and the bare-Git update fixture now exercises a real worktree-free server.
